# what is root bin ?

#  /bin — Essential User Commands

/bin contains essential command-line programs required by users.

[*] Basic Linux commands ke executable programs traditionally /bin mein hote hain.

# Examples:

ls

cp

mv

cat

rm

Modern Linux distributions mein /bin often /usr/bin ka symlink hota hai.
