# what is root user ?
# . /usr — User Applications & Programs

/usr contains many user-space programs, libraries and shared resources.

[*] /usr mein applications, commands, libraries aur other user-space resources ka large portion hota hai.

# Important directories:

/usr/bin
/usr/sbin
/usr/lib
/usr/share

# Example:

ls /usr/bin
