# what is /root - user's home directory ?
/root — Root User's Home

/root is the home directory of the root user.

[*] root administrator user hota hai aur uska home directory /root hota hai.

# Important difference:

/root    → root user's home

/       → entire filesystem

